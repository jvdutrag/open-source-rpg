import { createTheme } from '@mui/material/styles'

const theme = createTheme({
    palette: {
        mode: 'dark',
        primary: {
            main: '#659ec2'
        },
        secondary: {
            main: '#858585'
        }
    }
});

export default theme
